{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "inactivity_reminder_days": {
      "type": "number",
      "description": "Days of inactivity before sending a reminder email (1\u20133, default 3)",
      "default": 3,
      "minimum": 1,
      "maximum": 3
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}